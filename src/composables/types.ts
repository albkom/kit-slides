// ── Adapter marker contract ─────────────────────────────────────────────────
// The kit ships only the marker interface. Concrete fetch methods and the
// domain row/computed shapes live in the consumer deck, which extends this
// to describe its own data contract.

export interface IAdapter {}
